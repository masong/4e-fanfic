<% if story != nil %>
  <div class=story>
    <div id=title class=highlight-text>
      <%= story.title %>
    </div>
    <div <% if !add_underline %> id=last-story <% end %> class=story-body>
      <% if story.body != nil %>
        <%= story.body.gsub(/\n/, '<br/>').html_safe %>
      <% end %>
    </div>
  </div>
<% end %>
