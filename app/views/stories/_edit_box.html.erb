<%= form_for @story, :url => {:action => 'save', :id =>@story.id} do |f|%>
  <div>
    <%= f.label :body %>
    <%= f.text_field :body, :value => ''%>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
